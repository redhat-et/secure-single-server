bind 0.0.0.0
port 6379
protected-mode yes
dir /data

# Preserve admitted/reconciled token usage across service restarts. With
# appendfsync everysec, a host crash can lose approximately one second of data.
appendonly yes
appendfsync everysec
save ""

aclfile /run/secrets/users.acl
